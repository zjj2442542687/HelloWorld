import java.util.Arrays;
import java.util.Scanner;

公共课t1 {

	public static void main（String [] args）{
       Scanner lnt = new Scanner（System.in）;
       int m = lnt.nextInt（）;
       int n = lnt.nextInt（）;
       int [] a = new int [n];
       int sum = 0，count = 0;
       for（int i = 0; i <n; i ++）{
    	   A [1] = lnt.nextInt（）;
       }
       Arrays.sort（一）;
       for（int i = 0; i <n; i ++）{
    	   总和+ = A [1];
    	   if（sum <= m）count ++;
       }
       的System.out.println（计数）;
	}
}


贪心算法（又称贪婪算法）是指，在对问题求解时，总是做出在当前看来是最好的选择。也就是说，不从整体最优上加以考虑，他所做出的是在某种意义上的局部最优解
这个题就是海盗要带回数量最多的宝物，就要找最轻的宝物开始带走，所以先给宝物的重量排序，再从重量最小的开始带。直至数量达到他能带走的最大重量。
